describe('publish()', () => {
  test.todo('should publish packages normally');
  test.todo('should publish packages with the registry option');
  test.todo('should publish packages with the tag option');
});
