describe('getSharedPackageJson()', () => {
  test.todo('should get blank package.json content if there is no shared package.json config');
  test.todo('should get shared package.json content from shared package.json config');
});
