//const defaultArgv: DesktopappArgv = {
//  command: 'start',
//  app: 'app',
//  output: undefined,
//  staticFileDirectories: undefined,
//  staticFilePackages: undefined,
//};
//const zeroconfigPath: string = path.resolve(__dirname, '../../');

describe('createDesktopappConfig()', () => {
  test('기본 Desktopapp Config를 만든다', async () => {
    //const argv: DesktopappArgv = {
    //  ...defaultArgv,
    //};
    //const cwd: string = await createTmpFixture('')
  });
});
