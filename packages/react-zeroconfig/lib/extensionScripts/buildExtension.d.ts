import { ExtensionConfig } from '../types';
export declare function buildExtension({ cwd, app, zeroconfigPath, staticFileDirectories, output, extend, entryFiles, vendorFileName, styleFileName, }: ExtensionConfig): Promise<void>;
